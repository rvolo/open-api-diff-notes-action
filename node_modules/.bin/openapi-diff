#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../openapi-diff/bin/openapi-diff" "$@"
  ret=$?
else 
  node  "$basedir/../openapi-diff/bin/openapi-diff" "$@"
  ret=$?
fi
exit $ret
