image: node:10.16.0

pipelines:
  default:
    - step:
        name: 'Default'
        script:
          - ./build/ci-default.sh
        services:
          - docker
  branches:
    master:
      - step:
          name: 'Master'
          script:
            - ./build/ci-master.sh
          services:
            - docker
