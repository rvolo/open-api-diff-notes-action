'use strict';

const hooks = require('hooks');

hooks.before('/diffSchemas > POST > 400 > application/json; charset=utf-8', (transaction) => {
    const body = JSON.parse(transaction.request.body);
    body.sourceSchema = {not: 'a-json-schema'};
    transaction.request.body = JSON.stringify(body);
});
