'use strict';

const bunyan = require('bunyan');
const packageJson = require('../package.json');

const log = bunyan.createLogger({name: packageJson.name});

module.exports = {
    info: (code, message, userDetails) => {
        const details = userDetails || {};

        details.code = code;
        log.info(details, message);
    }
};
